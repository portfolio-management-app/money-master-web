import { observer } from "mobx-react-lite";

interface IProps {
    handleFormSubmit: Function;
}

const CSDMoveToFundForm = observer(({ handleFormSubmit }: IProps) => {
    return <></>;
});

export default CSDMoveToFundForm;